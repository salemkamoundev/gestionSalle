export interface PackServiceItem {
  nom: string;
  description?: string;
  prix: number;
}

export interface Pack {
  id?: string;

  nom: string;
  description?: string;

  staffIds?: string[]; // employés
  teamIds?: string[];  // équipes
  services?: PackServiceItem[];


  // NOUVEAU: prix total calculé à partir des services du pack
  price?: number;
  active: boolean;
  createdAt?: string;
}
